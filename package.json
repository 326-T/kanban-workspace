{
  "name": "kanban-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "engine": "bun packages/engine/src/index.ts",
    "web:dev": "bun run --filter @kw/web dev",
    "web:build": "bun run --filter @kw/web build"
  },
  "devDependencies": {
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.3.3"
  }
}
