{
  "name": "kanban-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "engine": "bun packages/engine/src/index.ts",
    "web:dev": "bun --cwd packages/web run dev",
    "web:build": "bun --cwd packages/web run build"
  },
  "devDependencies": {
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.3.3"
  }
}
